<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
    <body>
        <h1>The Game Database</h1>
        <ul>
            <% if (games) {%>
                <h1>All Games Currently Recorded</h1>
                <% games.forEach(game => { %>
                    <li>
                        ID: <%= game.id %> 
                        Title: <%= game.title %> 
                        Released: <%= game.formattedDate %>
                        <a href="/update/games/<%= game.id %>">Update</a>
                        <form action="/delete/games/<%= game.id %>" method="POST" style="display:inline;">
                            <button type="submit" onclick="return confirm('Are you sure you want to delete this data?');">Delete</button>
                        </form>
                    </li>
                <% }); %>
            <% } %>
        </ul>
        <br>
        <h1>All Genres Currently Recorded</h1>
        <p><%= genres %></p>
        <br>
        <h1>All Developers Currently Recorded</h1>
        <p><%= developers %></p>
        <p><%= founded %></p>
        <br>
        <a href="/manage">Game List Manager</a>
    </body>
</html>